---

installation_root_folder: "C:\\Experitest"
installation_folder: "{{ installation_root_folder }}\\{{ app_name }}-{{ app_version }}"

temp_folder: "c:\\temp\\experitest"

service_name: "{{ app_name }}"

installer_file_name: "dist-Windows-SIGNER-{{ app_version }}.zip"

relative_path: "app-signer/windows"

launcher_file_name: "start.bat"
